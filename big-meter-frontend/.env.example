# Big Meter Frontend - Environment Variables
# Copy this file to .env.local for local development
# Copy this file to .env.production for production deployment

# ============================================
# API Configuration
# ============================================

# Backend API Base URL
# The base URL for the Big Meter backend API
# Development: http://localhost:8089
# Production: https://your-production-api.pwa.co.th
VITE_API_BASE_URL=http://localhost:8089

# PWA Intranet Login API
# The authentication endpoint for PWA Intranet login
# This should point to the PWA webservice login endpoint
# Default: https://intranet.pwa.co.th/login/webservice_login6.php
# Leave commented to use the default value, or uncomment to override
# VITE_LOGIN_API=https://intranet.pwa.co.th/login/webservice_login6.php

# ============================================
# Notes
# ============================================
#
# - All environment variables must be prefixed with VITE_ to be exposed to the client
# - The dev server will proxy /api requests to VITE_API_BASE_URL
# - The dev server will proxy /auth/login requests to VITE_LOGIN_API
# - For production builds, ensure HTTPS URLs are used
# - CORS must be configured on the backend to allow requests from your frontend domain
